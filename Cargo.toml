[package]
name = "ruspiro-allocator"
authors = ["André Borrmann <pspwizard@gmx.de>"]
version = "0.3.0" # remember to update html_root_url
description = """
Simple and lightweight heap memory allocator for Raspberry Pi baremetal environments.
"""
license = "Apache-2.0"
repository = "https://github.com/RusPiRo/ruspiro-allocator/tree/v0.3.0"
documentation = "https://docs.rs/ruspiro-allocator/0.3.0"
readme = "README.md"
keywords = ["RusPiRo", "baremetal", "allocator", "raspberrypi"]
categories = ["no-std", "embedded"]
edition = "2018"

[badges]
travis-ci = { repository = "RusPiRo/ruspiro-allocator", branch = "master" }
maintenance = { status = "actively-developed" }

[build-dependencies]
cc = "1.0.37"

[lib]

[dependencies]
ruspiro-lock = { path = "../lock", version = "0.3" }

[features]

